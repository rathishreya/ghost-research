# Losing Patterns — cross-project memory

This file collects creative patterns that *failed* across past campaigns. Auto-read by `/design-ads` so we don't repeat mistakes.

_(Empty for now. Will fill in as `/decide-action` finds losers and logs them here.)_

## Format

When you add an entry:

```
## [Pattern name]
- **Format:** [video / static / carousel]
- **Platform:** [Meta / LinkedIn / etc]
- **What we tried:** [1-2 sentences]
- **Why it failed:** [specific reason — wrong emotion, weak hook, mismatched audience, etc.]
- **Evidence:** [slug] — CTR [X]%, CPL $[Y]
- **Avoid when:** [conditions to watch for]
```
